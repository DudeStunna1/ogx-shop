{
  "name": "ogx-shop",
  "version": "1.0.0",
  "private": true,
  "description": "SHOP.OS baseline runtime for OGX Shop",
  "main": "worker.js",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check worker.js services/*.js tests/*.js"
  },
  "engines": {
    "node": ">=20"
  }
}
